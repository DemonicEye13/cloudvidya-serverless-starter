# boto3 ships inside the AWS Lambda Python runtime already, so this file is
# intentionally near-empty — it exists so `sam build` has somewhere to look,
# and so you have an obvious place to add packages if your extension needs one.
